apiVersion: apps/v1
kind: Deployment
metadata:
  name: supremacy-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: supremacy-frontend
  template:
    metadata:
      labels:
        app: supremacy-frontend
    spec:
      containers:
      - name: supremacy-frontend
        image: supremacy.azurecr.io/frontend
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: supremacy-frontend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: supremacy-frontend
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: supremacy-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  tls:
  - hosts:
    - '*.jasoncabot.me'
    secretName: jasoncabot-me
  rules:
  - host: supremacy.jasoncabot.me
    http:
      paths:
      - backend:
          serviceName: supremacy-frontend
          servicePort: 80
