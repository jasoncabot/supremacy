import { useSelection as useSelectionContext } from './SelectionContext';

// Re-export the hook with a more descriptive name
export const useSelection = useSelectionContext;

export default useSelection;
